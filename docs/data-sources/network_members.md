---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "hcloud_network_members Data Source - hcloud"
subcategory: ""
description: |-
  Provides a list of Hetzner Cloud Network Members.
  See the Networks API documentation https://docs.hetzner.cloud/reference/cloud#tag/networks for more details.
---

# hcloud_network_members (Data Source)

Provides a list of Hetzner Cloud Network Members.

See the [Networks API documentation](https://docs.hetzner.cloud/reference/cloud#tag/networks) for more details.

## Example Usage

```terraform
data "hcloud_network_members" "by_type" {
  network_id = hcloud_network.example.id
  with_type  = ["load_balancer", "server"]
}

data "hcloud_network_members" "by_status" {
  network_id  = hcloud_network.example.id
  with_status = ["ok", "attaching", "detaching", "updating", "error"]
}

data "hcloud_network_members" "by_subnet" {
  network_id  = hcloud_network.example.id
  with_subnet = ["10.0.1.0/24"]
}

data "hcloud_network_members" "all" {
  network_id = hcloud_network.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_id` (Number) ID the Network to list the members of.

### Optional

- `with_status` (Set of String) Filter members by status. May be used multiple times.
- `with_subnet` (Set of String) Filter members by subnet. May be used multiple times.
- `with_type` (Set of String) Filter members by type. May be used multiple times.

### Read-Only

- `members` (Attributes List) (see [below for nested schema](#nestedatt--members))

<a id="nestedatt--members"></a>
### Nested Schema for `members`

Read-Only:

- `alias_ips` (Set of String) Additional IP addresses of the Resource within the Network.
- `id` (Number) ID of the Resource attached to the Network.
- `ip` (String) IP address of the Resource within the Network.
- `status` (String) Status of the Resource within the Network.
- `subnet` (String) IP range of the subnet the Resource is attached to.
- `type` (String) Type of the Resource attached to the Network.
